# Move to Doing Example

User message:
`Take the backlog item about docs cleanup now.`

AI backlog action:
- move the item from `TODO` to `DOING`
- confirm route and validation path

Resulting note:
`VCP-015 moved to DOING. Validation required: check-newlines + validate-links.`
