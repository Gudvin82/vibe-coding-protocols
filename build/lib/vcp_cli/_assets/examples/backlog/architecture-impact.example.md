# Architecture Impact Example

User message:
`Add queue-based retry handling for failed webhooks.`

AI backlog action:
- create backlog item
- architecture impact: `cross-layer`
- require `PROJECT_MAP.md` and `ARCHITECTURE_SOURCE_OF_TRUTH.md` update

Linked route:
`Operations` or `Hardening`, depending on production sensitivity.
