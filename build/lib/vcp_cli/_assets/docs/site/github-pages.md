# GitHub Pages Readiness

Options:
- publish `/docs` with a lightweight static approach;
- keep markdown as source of truth;
- add Pages workflow only when navigation and publishing ownership are stable.

This pass does not require publishing Pages.
