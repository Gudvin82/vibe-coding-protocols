# Vibe Coding Protocols v0.7.1 — Public Version Clarity Patch

v0.7.1 is a narrow patch release focused on public version clarity and external AI interpretation safety.

Included in this release:

- current first-contact surfaces now name the GitHub repository release as `Vibe Coding Protocols v0.7.1`;
- README and README_ru no longer expose `v1.4` as a current release label on the first screen;
- CLI headline output now leads with `Repository package: v0.7.1`;
- AI-facing docs and manifests now treat `v1.4` only as an internal stable methodology reference;
- CHANGELOG and release metadata now surface `v0.7.1` as the current top visible package release.

Boundaries preserved:

- no new product features;
- no retag of the previously published release tag;
- no claim that `v1.4` is the current repository package;
- no new PyPI or npm publication claims;
- no rewrite of historical release documentation beyond current release indexing.
