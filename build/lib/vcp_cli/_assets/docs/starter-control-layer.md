# Starter Control Layer

`templates/starter-control-layer/` is a copyable control-layer starter, not an application starter.

Goal:
- two files to read;
- one folder to copy;
- immediate review, PR Gate, and project-memory surfaces.

It does not ship a SaaS application, telemetry, or cloud sync.
