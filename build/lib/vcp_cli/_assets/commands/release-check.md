# Release Check Command

Use before tagging or publishing a release.

Prompt:

Prepare a release-readiness check.

Return:
- changed artifacts;
- validation results;
- known warnings;
- migration notes required;
- whether `AGENTS.md` or prompts should be copied forward by users;
- whether release notes are consistent with the repository state.

Do not publish anything automatically.
