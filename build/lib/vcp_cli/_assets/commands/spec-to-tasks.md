# /spec-to-tasks

Convert an approved feature spec into concrete tasks and backlog linkage.

Inputs:
- feature spec
- acceptance criteria
- validation plan

Output:
- task breakdown and backlog-ready slices
