# Feature Spec

`vcp-version: v0.5.9`

Purpose: convert an approved PRD into a feature-level execution spec.

A feature spec should include:
- user and operator flows
- system boundaries
- dependencies
- acceptance criteria
- failure modes
- validation plan
- rollout or release notes if applicable

The feature spec should be narrow enough that tasks can be derived without guessing hidden requirements.
