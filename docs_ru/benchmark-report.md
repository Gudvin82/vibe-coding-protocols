
# Benchmark Report

Текущий пакет репозитория: `v0.9.4`

Benchmark counts для этого релиза синхронизируются через
`.vcp/proof-counts.json`.

Проверка:

```bash
python3 -m vcp_cli benchmark run --json
```
