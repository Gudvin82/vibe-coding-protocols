# PR Gate

PR Gate — это локальная approval model и workflow template surface.

Используй перед launch, чтобы разделить `warn` и `block`, а не для автоматических обещаний merge control или certification.
