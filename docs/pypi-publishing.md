# PyPI Publishing

PyPI publication is still a manual future path.

`v0.9.0` does not claim a public PyPI release.

Local packaging and install verification are supported, but public publication must be explicitly performed and approved.
