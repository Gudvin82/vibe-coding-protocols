# Event Schema

Repository package: `v0.8.6`

The VCP event schema is a normalized record format for findings, warnings, transitions and gate failures.

It is meant for:
- review findings
- production error capture
- backlog transitions
- diagnostic warnings
- release gate failures

It is not:
- an IDS/IPS engine
- a SIEM product
- a production monitoring platform clone
