# Spec Retrofit

Spec retrofit is VCP's answer for existing projects that already have code but lack usable specs.

This is especially relevant for:
- AI-generated MVPs
- production systems with no PRD
- repos where the team must understand current behavior before changing it

The goal is to document observed behavior and gaps before implementation, not to rewrite source code.
