
# Anti-Chaos Recovery Kit

Use this route when AI already generated too much and trust is low.

Flow:
freeze changes -> snapshot current state -> identify active surfaces ->
run route recommender -> create risk register -> create audit backlog ->
choose recovery track -> PR Gate -> evidence bundle -> release or hold
decision.

No automatic cleanup. No automated rewrite. No security guarantee.
