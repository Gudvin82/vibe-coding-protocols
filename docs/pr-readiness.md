
# PR Readiness

PR Readiness makes the PR story concrete without creating PRs
automatically.

It checks whether:
- change intent exists;
- affected surfaces are listed;
- tests were run or explicitly not run;
- trust-check status is visible;
- evidence bundle inputs exist;
- release decision state is visible;
- required human approval is called out.
