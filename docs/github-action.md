# GitHub Actions

VCP ships workflow templates, not a Marketplace action.

For PR Gate setup, copy from:
- `ci-examples/github-actions/vcp-pr-gate.yml`

Expected outputs:
- review-diff summary;
- release-check result;
- optional dashboard artifact upload if your repository already uses artifacts.
