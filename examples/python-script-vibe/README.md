# python-script-vibe

> Synthetic example — not a real production project.
> Use it to understand the workflow, not as a production template.

Tiny Python example for a minimal script-shaped project.

It demonstrates:
- lightweight project memory;
- a small `PROJECT_MAP.md`;
- a minimal `AUDIT_BACKLOG.md`;
- how to keep even tiny AI-generated code reviewable.
