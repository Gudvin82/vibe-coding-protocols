# PR Gate Hand-off

Use this placeholder in the target repository to document:
- which VCP route is active;
- which checks are required before merge;
- which risks still need human review.

See `docs/pr-gate.md` in the VCP source repository for the full model.
