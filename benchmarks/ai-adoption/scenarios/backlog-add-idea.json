{
  "scenario_id": "backlog-add-idea",
  "target_profile": "backlog",
  "target_signals": [
    "a new idea arrived during planning",
    "implementation has not started yet"
  ],
  "expected_route": "project-backlog-workflow",
  "expected_adoption_pack": "backlog",
  "required_files_to_inspect": [
    "PROJECT_BACKLOG.md",
    "docs/project-backlog.md",
    "templates/PROJECT_BACKLOG.md"
  ],
  "files_not_to_copy": [
    "templates/AUDIT_BACKLOG.md"
  ],
  "expected_stop_conditions": [
    "the idea is silently kept only in chat",
    "the work really belongs in AUDIT_BACKLOG"
  ],
  "expected_review_gate": "Required only when the idea becomes meaningful implementation work.",
  "expected_first_safe_action": "Add the idea to TODO before implementation starts.",
  "expected_validation_commands": [
    "python3 -m vcp_cli backlog add --title \"...\" --type idea --priority P3 --dry-run --json",
    "python3 -m vcp_cli backlog validate"
  ]
}
