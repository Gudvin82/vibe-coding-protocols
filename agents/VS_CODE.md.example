# VS_CODE.md example

Use the repository as a planning toolkit first:
- read `README.md`, `AGENTS.md` and `PROJECT_MAP.md`;
- keep changes reviewable;
- document follow-up in `AUDIT_BACKLOG.md`.

Before broad edits, ask the AI to produce a compact evidence map first.
Do not let it scan and rewrite the project in one step.
