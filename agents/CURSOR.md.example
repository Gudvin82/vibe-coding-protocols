# CURSOR.md example

Defaults:
- keep diffs small;
- use file references and changed-files plans;
- stop on risky auth, DB, dependency or workflow changes.

Before broad edits, ask the AI to produce a compact evidence map first.
Do not let it scan and rewrite the whole project in one step.
